program machine_epsilon
    implicit none
    real :: epsilon

    epsilon = 1.0
    do while (1.0 + epsilon > 1.0)
        epsilon = epsilon / 2.0
    end do

    print *, 'Машинный эпсилон:', epsilon

end program machine_epsilon
