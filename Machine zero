program machine_zero
    real :: zero

    zero = 1.0
    do while (zero / 2.0 > 0.0)
        zero = zero / 2.0
    end do

    zero = zero * 2.0

    print *, 'Machine zero:', zero

end program machine_zero
